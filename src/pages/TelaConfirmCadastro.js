import * as React from 'react';
import { TextInput, View, Text, Button, StyleSheet, TouchableOpacity, ScrollView } from 'react-native';

export default function TelaConfirmCadastro(){
    return(<>
    
    </>
    );
}